import type { FastifyReply, FastifyRequest } from "fastify";
export declare class SmsController {
    resolveRecipients(request: FastifyRequest, reply: FastifyReply): Promise<never>;
    previewDispatch(request: FastifyRequest, reply: FastifyReply): Promise<never>;
}
